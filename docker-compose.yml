version: '3'
services:
  flutter:
    container_name: flutter
    build: .
    stdin_open: true
    tty: true
    network_mode: "host"
    volumes: 
        - ./:/home/default/src
    working_dir: /home/default/src