version: '3'
services:
  flutter:
    container_name: flutter
    build: .
    stdin_open: true
    tty: true
    network_mode: "host"
    volumes: 
        - ./:/home/developer/src
    working_dir: /home/developer/src